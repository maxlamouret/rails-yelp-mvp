<h1>Restaurant#index</h1>
<% @restaurants.each do |restaurant| %>
  <h2><%= restaurant.name %></h2>
  <p><%= restaurant.address %></p>
  <%= link_to "see the detail", restaurant_path(restaurant) %>
<% end %>


  <%= link_to "Add a restaurant", restaurant_path(restaurant) %>

